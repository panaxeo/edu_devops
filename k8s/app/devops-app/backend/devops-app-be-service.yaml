apiVersion: v1
kind: Service
metadata:
  name: devops-app-backend-clusterip
  namespace: development
  labels:
    app.kubernetes.io/name: devops-app-backend-clusterip
    app.kubernetes.io/version: "1.0.0"
spec:
  type: ClusterIP
  selector:
    app: devops-app-backend
  ports:
    - port: 8080
      targetPort: 8080