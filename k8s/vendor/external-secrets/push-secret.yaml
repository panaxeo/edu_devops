# # TEST
# # kubectl create secret generic test-push-secret -n development --from-literal=key1=value1

# apiVersion: external-secrets.io/v1alpha1
# kind: PushSecret
# metadata:
#   name: test-push-2
#   namespace: development
# spec:
#   updatePolicy: Replace # Policy to overwrite existing secrets in the provider on sync
#   deletionPolicy: Delete # the provider' secret will be deleted if the PushSecret is deleted
#   refreshInterval: 10s
#   secretStoreRefs:
#     - name: k8s-store
#       kind: SecretStore
#   selector:
#     secret:
#       name: test-push-secret
#   data:
#     - match:
#         remoteRef:
#           remoteKey: test-push-secret-remote
#           property: new-key # the property to use to push into