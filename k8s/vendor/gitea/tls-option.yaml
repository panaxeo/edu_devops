apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
spec:
  # certificates:
  #   - secretName: gitea-tls-secret                      
  defaultCertificate:
    secretName: gitea-tls-secret