apiVersion: v1
kind: Service
metadata:
  name: traefik-web-service
  namespace: ingress-traefik
spec:
  type: LoadBalancer
  selector:
    app: traefik
  ports:
    - targetPort: web
      port: 80